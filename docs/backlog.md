# Бэклог идей — мини-приложение MAX для учеников и родителей

## MVP (первая версия)
- [ ] Проверка домашней работы по фото тетради (1 или несколько фото)
- [ ] Поиск задачи из учебника по фото/тексту + решение с учётом класса обучения
- [ ] Точная идентификация учебника (класс → предмет → учебник → номер задания), т.к. номера задач различаются между изданиями
- [ ] Профиль ученика: класс, предметы
- [ ] Базовый родительский просмотр (что делал ребёнок, какие задачи решал)

## Точность решений (ключевое требование — уровень ГДЗ)
- [ ] Решение «с нуля» на основе условия из фото/текста пользователя — НЕ через сбор и хранение чужих учебников и ГДЗ целиком (юридически рискованно)
- [ ] Собственная библиотека методических паттернов по темам школьной программы («как решать задачи на движение» и т.п.) — общие методы, а не чужие произведения
- [ ] Собственная верифицированная база ответов, растущая органически из решений самой системы (не скачанная с чужих ГДЗ-сайтов)
- [ ] Интеграция с Wolfram Alpha API (и/или SymPy бесплатно) для проверки математических/физических ответов
- [ ] Генерация решения несколькими способами и сверка совпадения (сигнал уверенности)
- [ ] Кнопка «ответ неверный» → отправка на ручную проверку экспертом → исправление базы
- [ ] Уровень объяснения должен соответствовать пройденным на момент задания методам по программе класса
- [ ] Купленные ГДЗ/учебники — использовать только как внутренний эталон для QA (сверка финального ответа), НЕ копировать текст объяснений в продукт
- [ ] Показывать пользователю распознанный текст условия/решения ДО выдачи ответа с возможностью исправить (защита от ошибок OCR)

## Кэширование и оптимизация затрат на AI
- [ ] Канонический ключ задачи: учебник + номер задания (если распознаётся на фото), иначе — нормализованный хэш текста условия
- [ ] Проверка кэша перед обращением к AI: найдено → мгновенный бесплатный ответ; не найдено → полный AI-пайплайн → сохранение результата в базу
- [ ] Разделение моделей по задачам: дешёвая/быстрая — для OCR и извлечения условия, дорогая/точная — только для решения новых (некэшированных) задач

## Цикл самоулучшения точности
- [ ] Eval-пайплайн: после каждого решения сверять финальный ответ с эталоном (Wolfram/SymPy для точных наук, купленные ГДЗ/эксперт — для остальных предметов)
- [ ] Логирование расхождений: тема, класс, тип ошибки (OCR/метод/вычисление)
- [ ] Донастройка промптов/логики решения по проблемным темам на основе логов
- [ ] Дообучение на собственных проверенных решениях (не на тексте чужих ГДЗ) по мере накопления верифицированной базы
- [ ] Дашборд точности по предмету/классу/теме для приоритизации доработок

## UX для поколения альфа
- [ ] Вертикальный свайповый интерфейс — решение показывается по шагам (как сторис/рилс), а не сплошным текстом
- [ ] Анимация ключевых шагов решения
- [ ] Короткие карточки, крупный шрифт, минимум текста на экран

## Backlog — идеи на будущее

### Для ученика
- Пошаговые подсказки вместо готового ответа («режим самостоятельного решения» — открывать шаги по одному)
- Голосовое объяснение решения
- Разные уровни объяснения («как для 5 класса» / «подробно» / «кратко»)
- Тренажёр по слабым темам на основе истории ошибок
- Геймификация: баллы, стрики, значки за регулярные занятия
- Проверка сочинений и текстовых работ на грамотность (не только точные науки)
- Поддержка нескольких предметов (математика, физика, русский, английский и т.д.)
- Оффлайн-кэш последних решённых задач

### Для родителя
- Дашборд прогресса ребёнка (по предметам, по времени)
- Уведомления в бот MAX о статусе ДЗ / выполненных задачах
- Поддержка нескольких детей в одном родительском аккаунте
- Еженедельный отчёт (сводка успеваемости)
- Экспорт отчёта в PDF для учителя

### Для учителя (возможная будущая роль)
- Режим «контрольная работа» — временное отключение прямых ответов, чтобы предотвратить списывание
- Проверка и комментирование работ учеников
- Просмотр статистики по классу

### Интеграции и инфраструктура
- Интеграция с электронным дневником школы (если есть открытое API)
- API для интеграции со школьными CRM/системами
- Уведомления через MAX-бота (напоминания о ДЗ, дедлайны)

## Открытые вопросы / риски (требуют решения до старта разработки)
- **Юрлицо/ИП/самозанятый**: подключение к платформе «MAX для партнёров» и публикация мини-приложения доступны только резидентам РФ с таким статусом — нужно определить, на кого регистрировать бота
- **Авторские права на учебники**: нельзя просто сканировать и распространять содержимое учебников целиком — нужна либо собственная база задач, либо генерация аналогичных задач, либо лицензия от издательства
- **Массовый сбор всех учебников и всех ГДЗ в свою базу — недопустим**: это оптовое копирование чужих охраняемых произведений (и учебников, и решебников). Вместо этого — решать задачи самостоятельно (по фото/тексту конкретного пользователя) и накапливать свою верифицированную базу органически, а не скачивать чужой контент
- **Копирование чужих ГДЗ-сайтов недопустимо**: решения нужно генерировать/проверять самостоятельно, а не копировать тексты с существующих решебников — у издательств (напр. «Просвещение») были прецеденты претензий к сайтам-решебникам
- **152-ФЗ и данные детей**: фото тетрадей — персональные данные несовершеннолетних; нужны политика конфиденциальности, согласие родителя, минимизация хранения фото (например, удалять после обработки)
- **Точность распознавания почерка**: особенно у младших классов — нужен fallback на ручной ввод текста, если OCR не справился
- **Хостинг**: мини-приложение требует HTTPS-хостинга статики (VK Cloud / GitHub Pages / Yandex Cloud) + отдельный бэкенд-сервер для AI-обработки.
  С 30.08.2026 аргумент стал жёстче: экран настройки берёт список предметов
  с бэкенда (GET /api/subjects), при недоступном туннеле пользователь не
  увидит предметов вообще — без постоянного сервера приложение мертво
  с первого экрана

## Gating по подписке на канал (сделано: инфраструктура, 29.08.2026)
- [x] chatId канала добыт (-74564545637740, из события bot_added через long polling)
- [x] Механика подписки проверена на живом API: не-участник = 200 + пустой members
- [x] backend/src/subscription.js: checkSubscription + мидлвара, off/shadow/on,
      кэш, fail-open, интерлок со STRICT_INIT_DATA, старт-проверка канала
- [ ] Прогон shadow на реальном трафике (закрывает непроверенный случай:
      существующий, но не подписанный аккаунт)
- [ ] Экран «подпишись на канал» на фронте (обработка 403 not_subscribed)
- [ ] Включение STRICT_INIT_DATA=on + SUBSCRIPTION_GATING=on на проде
Факты и грабли: docs/project-handoff.md (хост platform-api, не api2; токен заголовком).

## Качество распознавания (29.08.2026)
- [x] Инструмент ocr-eval (посимвольный дифф, нормализация LaTeX-нотации, --offline)
- [x] Замер на 19 реальных фото; вывод: confidence не сигнализирует ошибки цифр
      (ma-02: 0.78 при 8 ошибках) — гейтов на confidence не строить
- [x] Misread-детектор за флагом MISREAD_DETECTION, лог-only; ограничения:
      только числовые равенства + слепота к систематической подмене
- [ ] Вычитать черновые эталоны eval-data руками (поля note в truth.json)
- [ ] Статистика срабатываний misread на реальном трафике → решить, что показывать
      пользователю (как «ошибка распознавания» показывать НЕЛЬЗЯ — не различает
      misread и настоящую ошибку ученика)

## Замер модели solver (30.08.2026)
- [x] Инструмент model-compare (Opus vs Haiku, свои задачи, SymPy-сверка ответа)
- [x] 110 типовых задач 1–11 класс: обе модели без математических ошибок,
      Haiku втрое дешевле ($0.0087 vs $0.0252/задача). Детали в handoff.
- [x] Багфиксы по следам: «или»/«и» в parseCandidateAnswer, NO_ROOTS,
      ведущий ноль в numbersToRationals
- [ ] Замер на многошаговых задачах (вторая часть ОГЭ/ЕГЭ, геометрия
      с построением, доказательства) — без него модель в проде не менять
- [ ] Если многошаговый замер тоже ок — решить про SOLVER_MODEL=haiku
      (или маршрутизацию: простые задачи → Haiku, сложные → Opus)

## Предметы по классам (сделано 30.08.2026, проверено на телефоне)
- [x] subjects.json по ФУП (учебный год 2026/27; пересмотр при изменении ФОП)
- [x] GET /api/subjects?grade=N + валидация пары в solve.js и checkHomework.js
      (невозможная пара → 400)
- [x] Фронт: список с бэкенда, скрытие до выбора класса, скелеты, сброс
      невалидной пары, ошибка сети с повтором, защита от гонки запросов

## Пробелы промпта, найденные замером prompt-gaps (фиксация 30.08.2026, чинить после замера)
Источник: слепое судейство решений по (класс, предмет), начиная с 1 класса.
Оценка судьи субъективна (модель), но пункты ниже повторялись систематически.

1. [ ] **ПРИОРИТЕТ: LaTeX-доллары вокруг кириллицы в нематематических предметах**
       («$а$», «$4$ буквы» в решениях по русскому языку). Это баг, видимый
       пользователю: на фронте KaTeX отрендерит это как формулу с математическим
       начертанием. Чинится промптом solver (формулы только для математики).
2. [ ] Математический шаблон шагов («что дано», «проверка», нумерованные шаги)
       навязан устным предметам — русскому, чтению. Схема шагов в промпте
       писана под вычисления.
3. [ ] Ответ прячется в конец: для младших классов и устных предметов нужен
       короткий ответ сначала, потом объяснение.
4. [ ] Правило не называется явно («сколько гласных — столько слогов»,
       «на N больше = столько же и ещё N») — применяется молча, ученику
       не на что опереться в следующей задаче.

6. [ ] Нет школьного оформления записи: краткая запись условия
       («Было — Вышли — Вошли»), образец тетрадной записи («дуб — дубы (б)»,
       «Ответ: 18 см»), решение «1) … 2) …». Домашку сдают тетрадью.
7. [ ] Английский: нет произношения и чтения слов (в началке это половина
       предмета; у Opus вдобавок ошибка в транскрипции «three» → «фри»).
8. [ ] Литература: нет опоры на текст — цитат и ключевых слов произведения
       («владычица морская»), которые ждёт учитель.

9. [ ] Гуманитарная инверсия: перегруз фактами сверх вопроса (история 5 кл.:
       в ответ про клинопись приезжают Хаммурапи и Гильгамеш; в довесках —
       фактический дрейф: плуг рядом с мотыгой, кузнечное дело при земледелии).
10. [ ] Не используются опорные слова учебника: «дар Нила», «присваивающее →
       производящее»; решение говорит «Месопотамия», когда в условии
       «Междуречье». Учитель ждёт формулировок учебника.

Отдельно — НЕ дефект промпта, а продуктовая задача:
- [ ] **Наглядная опора** (числовой луч, счётные палочки, «домики» состава
      числа, дуги слогов, звуковые схемы) — самый частый пробел по мнению
      судьи (~20 упоминаний на 30 разборов 1 класса), но промптом не чинится:
      требует отрисовки схем на фронте (SVG/канвас или текстовые псевдосхемы).
      Аргументы копятся: (1) замер 8 класса — доказательства про диагонали
      четырёхугольников нечитаемы в текстовом канале (тройные обозначения
      углов ∠OAB/∠OCD судья назвал «трудночитаемыми без чертежа»; словесную
      расшифровку решили НЕ добавлять — с чертежом она станет шумом);
      (2) место под наблюдение 9 класса: алгебра-график и окружность бьют
      в ту же границу — дополнить после прогона.

## Наблюдения о моделях (из замера prompt-gaps, 2 класс) — НЕ пробелы промпта
- **Haiku ошибается на ТОНКОЙ фактуре, а не «на гуманитарных вообще»**
  (уточнено после 5 класса): промахи — точные формулировки правил (чк-чн
  раздуто до «после ч и щ ь не пишется»), детали сюжета (лесенка желаний
  в «Сказке о рыбаке и рыбке»), морфемный разбор («подснежник» с окончанием
  -ик). На фактах уровня учебника 5 класса (история/география/биология)
  не уступает Opus — 40/40. Вывод для будущей маршрутизации моделей:
  граница проходит по «есть ли объективный арбитр» (SymPy/эталон) и по
  требуемой точности формулировок, а не по предмету или сложности задачи.
- **РИСК: ложное обобщение правила опаснее неверного ответа** — неверный ответ
  ученик заметит по несовпадению, а неправильное правило унесёт дальше и будет
  применять к другим словам/задачам. Учесть при любом решении о дешёвой модели
  на гуманитарных предметах.

## Ограничение curriculum-слоя (замечено на 2 классе, пока не чинить)
- [ ] Белый список методов работает на уровне КЛАССА, но не ТЕМЫ: столбик
      формально разрешён во 2 классе, однако в теме «устный приём вычитания»
      неуместен — solver решил столбиком то, что должно решаться устно.
      Граница «метод разрешён vs уместен в этой теме» текущим белым списком
      не выражается.

## Ход замера prompt-gaps (обновлено 30.08.2026)
- 1–3 классы пройдены: 8 категорий пробелов (см. список выше), на 3 классе
  замер вышел на плато — новых категорий не появилось, только новые примеры.
- 4 и 6 классы пропущены ОСОЗНАННО (состав предметов повторяет соседние
  классы, новых находок не ожидалось) — решение пользователя, срезанный путь.
- План дальше: 5 класс (только новые предметы: история, география, биология
  + математика как контроль) → 7 класс полностью (алгебра, геометрия, физика,
  информатика; особое внимание: понятность геометрии БЕЗ чертежа и единицы
  измерения/размерность в физике) → дальше по обстановке.

## РИСК: брак генерации текста — смешение алфавитов и битые слова (НЕ пробел промпта)
Чинится не промптом, а пост-проверкой текста перед отдачей пользователю.
Пока только фиксация, не чинить.

Примеры из замера prompt-gaps (решения, которые увидел бы ученик):
- Opus, 5 класс, история: «самой長 длинной» (CJK-иероглиф внутри русского
  слова), «egyptянам» (латиница в слове), «перенялиьвавилоняне» (склейка).
- Haiku, 5 класс, история: «перевол», «с больше запасами» (аграмматизмы).
- Haiku, 2 класс, литчтение: «круглый и весёлый булочка».

Частота по классам (упоминания судьёй в 40 разборах на класс):
1 класс — 1 (haiku), 2 класс — 1 (haiku), 3 класс — 0, 5 класс — 5 (обе
модели). Похоже, провоцируется длинными русскоязычными гуманитарными
ответами. Возможное направление починки: дешёвый пост-проход проверки
текста (регэксп на смешение алфавитов + LLM-вычитка) перед показом.

## Слепое пятно наборов: один тип источника (наблюдение 31.08.2026)
Баг «задача с доски отклоняется как „не из учебника“» нашёлся ОБЫЧНЫМ
ИСПОЛЬЗОВАНИЕМ, а не замером и не канарейкой — и ни один наш замер его
бы не поймал: во всех наборах (ocr-eval, prompt-gaps, model-compare)
условия либо печатные из учебников, либо текстовые. Фото доски, карточки,
экрана, диктовки в наборах нет вовсе.
**Вывод для будущих замеров: наборы собраны из одного типа источника —
это слепое пятно. При следующем замере добавить нетипичные источники:
доска, карточка, экран, диктовка.** Родственен «уроку догонки»
(prompt-gaps-report): новые классы дефектов приходят из непохожести
природы задания, а не из трудности.

## Замер prompt-gaps закрыт (30.08.2026)
- Итоговая сводка: docs/prompt-gaps-report.md (13 категорий по типу починки,
  выводы по моделям, границы применимости, стоимость ~$13).
- Следующий шаг — правка промпта по категориям 1–11; план обсуждается
  отдельно, пока НИЧЕГО не чинилось.

## Графики функций (сделано 31.08.2026, проверено на телефоне)
- [x] verify_sympy mode=plot; поле graph в схеме solver с критерием в промпте;
      расчёт в solve-пути параллельно верификации; свой SVG на фронте
- [x] Фикс: арифметические ответы (int/float) проходят SymPy-верификацию

## Всплыло по дороге (не чинилось)
- [ ] **parseCandidateAnswer не берёт составные ответы** («нули: x = −1 и
      x = 3 (вершина (1; −4))») → лишние значения → verified false → решение
      не кэшируется. Графиковые формулировки провоцируют такие ответы чаще.
      Это ТРЕТЬЕ всплытие функции (было: «или»/«и» с кириллицей, узкий
      NO_ROOTS) — чинить основательно, а не заплаткой: пересмотреть разбор
      ответа целиком (возможно, отдельное структурированное поле ответа
      от solver вместо парсинга строки).
      Дополнение 31.08 (часть 2 чертежей): задачи на УГЛЫ дают составные
      ответы почти всегда — вертикальные три значения, смежные два
      («∠2 = 140°, ∠3 = 40°, ∠4 = 140°» → verified false, мимо кэша).
      К разбору телеметрии 10.09 пара (геометрия, углы) будет в лидерах
      unsupported/false — причина известна заранее, не новый класс.
- [ ] **Чертежи для геометрии — отдельная задача**, графиками функций не
      закрывается: там нужны треугольники/углы/обозначения, а не y = f(x)
      (см. пробел №8 в prompt-gaps-report: текст без чертежа не
      самодостаточен).

## Предметные блоки промпта (механизм с 31.08.2026)
- [x] src/data/subject-rules.js + подмешивание третьим system-блоком;
      кэш префикса подтверждён по usage (write→read при повторе пары)
- [ ] Расселить из SYSTEM_BASE три готовых предметных ветвления (формулы,
      структура шагов, оформление записи) по предметным блокам и сократить
      базу — КОГДА блоки докажут работоспособность на 5 новых правилах.
      База сейчас ~2000 токенов, каждое общее правило дороже предыдущего.

## Кэш решений: будущая book-форма ключа (записано 31.08.2026)
Ветка «book:учебник+класс+предмет+номер» УДАЛЕНА из buildCacheKey:
0 срабатываний из 84 записей за всю историю. **Механизм верный, мёртв
источник данных**: автор/название учебника не печатаются на странице
с задачей — vision физически не видит textbook, и совместное условие
«учебник И номер распознаны» недостижимо.
**Вернуть, когда появится профиль ученика** (пункт MVP «точная
идентификация учебника»): textbook — из ПРОФИЛЯ, номер задачи — из фото.
Такой ключ лучше хэша: один номер у одноклассников совпадает независимо
от качества фото и формулировок.

## Кэш промпта: база сравнения и триггеры пересмотра (31.08.2026)
**РЕШЕНИЕ Ильи: режим кэша не трогаем, смотрим на месяце живого трафика.
ДАТА ПЕРЕСМОТРА: начало октября 2026** — пересчитать на месячных данных
(SQL по зазорам запросов внутри троек уже написан, см. сессию 31.08).
В тот же пересмотр (решение Ильи): МНОГОУРОВНЕВЫЙ кэш промпта (общий
breakpoint на схему+SYSTEM_BASE, per-тройка хвост) и вопрос «нужна ли
ЧЕТВЕРТЬ в ключе кэша» — меряются теми же месячными данными.

База: 14 живых solve за 31.08, префикс солвера 21,5k токенов
(схема ~12,7k + SYSTEM_BASE 6k + блоки + program), Opus $5/M input:
- кэш ВЫКЛЮЧЕН: 14 × 21,5k × $5/M = **$1.51**;
- текущий 5m (факт дня: 10 холодных записей, 4 чтения): **$1.39**;
- часовой 1h (7 записей ×$10/M-эквивалент, 7 чтений): **$1.58** — дороже
  выключенного при сегодняшнем профиле.

Триггеры ЧИСЛОМ (p — доля запросов-повторов тройки в 5-минутном окне,
q — доля повторов в часовом окне):
- 5m окупается против выключенного при **p > 22%** (сегодня p = 4/14 ≈ 29%,
  5m уже чуть выгоден);
- 1h окупается против 5m при **q > (0.75 + 1.15p) / 1.9**; при сегодняшнем
  p≈29% это **q > 57%** (сегодня q = 7/14 = 50% — не окупается).
Смежный факт: цена 1h-записи ×2 от input (не ×1.25) — при включении
доучить usageCost (поле cache_creation.ephemeral_1h_input_tokens).

## П.5 часть А в работе (31.08.2026): интервальная сверка
- [x] setExpression в answerValues + mode=set в verify_sympy (свой белый
      список множеств, fail-closed) — интервалы/объединения verified.
- [ ] **УДАЛИТЬ 07.09.2026: временный лог живых expression-форм**
      (backend/expression-answers.log + блок [p5-log] в routes/solve.js) —
      включён на неделю для валидации набора форм настоящим трафиком.
- [ ] Часть Б (серии корней) — по живым формам из лога; ловушка канонизации
      периодов показана пробой (структурное == не работает, числовое окно
      требует общей нормализации периода).
- [ ] Часть В — ученические интервалы в check-пути (парсер «x∈(3;8]»).

## Оцифровка физики в curriculum — отложена по пробе (01.09.2026)
Находка: физика (и химия, информатика) живут без белого списка методов —
buildProgramBlock подставляет мягкую заглушку «ориентируйся на типичный
уровень класса». Проба-провокация (9 задач: физика 7–8 «на вырост» +
алгебра-контроль с白 списком, ~$0.35): **9/9 чисто, нарушений нет**.

Два факта из пробы (важнее самой пробы):
- Модель не просто избегает старших методов — она НАХОДИТ ОБХОДНЫЕ ПУТИ
  уровня класса: средняя скорость вместо кинематики 9 кл., средняя сила
  вместо интеграла. Это сознательный выбор уровня, не неспособность.
- Вывод «мягкие формулировки игнорируются» оказался НЕ универсальным:
  он про предметные требования к оформлению, а не про уровень сложности,
  где у модели есть собственное представление об уместном.

Известная граница (не дефект): h = g·t²/2 у 8-класса — серая зона стыка
программ (свободное падение в большинстве УМК — 9 класс); без
оцифрованного списка граница определяется представлением модели
о «типичном уровне», размытым на стыках классов.

Триггеры возврата к оцифровке:
1) жалоба на метод не по программе или замер, показавший нарушение;
2) оцифровка понадобится для чего-то ещё (например, карты типов задач
   из раздела ниже) — тогда ограничение методов станет побочной выгодой
   и вопрос пересматривается.

## Покрытие курса по типам задач — карта качества (решение Ильи 01.09.2026: не сейчас)
Обоснование: кэш покрытие курса НЕ наполняет (0/99 ловушек «та же форма,
другие числа» — числа в ключе), зато даёт карту слабых типов и детектор
слепых зон; прецедент — позиционные системы счисления, найденные ровно
так. Инструмент готов (eval-data/cache-probe, шаблоны с сидом), каркас
типов — 192 метода ФОП по алгебре 7–11 из curriculum; задача на дни.
Юр-граница: только СОБСТВЕННЫЕ условия-шаблоны (массовое предрешение
чужих номеров = массовое хранение чужих условий — под запретом skill).
**Вернуться после сентябрьских данных.**

## База кэш-прогона 01.09.2026 (сравнивать при изменениях верификатора/нормализации)
Прогон 33 ячейки × 17 задач = 561 (все точные науки), инструмент
eval-data/cache-probe (наборы шаблонные с сидом, чекпойнты):
- **verified 96% (537/561)** — против 13% живого трафика до П.5;
- **склейка вариаций 64% (127/198)**: поверхностные стили (без двоеточия,
  вводная строка, юникод-знаки — по 100%; переносы+пробелы 85%), глубокие
  словесные переформулировки 0% — честный предел метода;
- **ложных склеек 0 из 99 пар-ловушек** (числа, знаки, операции, «3:4» vs
  «34», «1,5» vs «15», «14:30»);
- экономия кэша $6.57 на 148 хитах, стоимость прогона ~$20.

- [ ] **Позиционные системы счисления — вне символьной сверки** (новый
      класс): «94₁₀ = 1011110₂» не формализуется в SymPy (двоичная запись
      читается десятичным числом). Информатика — единственный слабый
      предмет прогона (12–13/17). Решение понятное и дешёвое: сравнение
      через int(x, base) отдельным правилом. Не чиним сейчас.

## Разовая уборка кэша (когда-нибудь, не срочно)
После введения версии в ключ (01.09.2026) старые записи без префикса
версии и записи прежних версий нормализации остаются в solutions_cache
мёртвым грузом — безвредны (не находятся), но занимают место. Разовая
чистка `DELETE FROM solutions_cache WHERE cache_key NOT LIKE 'text:v3:%'`
(с актуальной версией) — когда таблица разрастётся. Сейчас ~1.2 МБ,
не срочно.

## Осознанные упрощения телеметрии экономики (31.08.2026)
- Стоимость СБОЙНЫХ запросов ≈ 0 в данных: при ошибке AI-вызова частичный
  usage запроса не пишется. Терпимо, пока сбои редки; при разборе расходов
  помнить, что реальный расход чуть выше записанного.

## Шум в логе: raw-body onAborted при обрыве POST (01.09.2026, не чиним)
Разбор алерта «Фоновые сбои»: в backend.log 18 стек-трейсов
`IncomingMessage.onAborted` (raw-body/index.js:245) — клиент оборвал POST,
пока express.json читал тело (15 МБ base64-фото). Это ДО роута, значит ДО
recognizeFromPhotos: **vision не запускался, денег не потрачено.** Процесс не
роняет, живым не видно. Только шум в логе — НЕ чиним (при желании — тихий
обработчик req.on('aborted'), но это косметика).

- [ ] **ОТДЕЛЬНАЯ, РЕАЛЬНАЯ потеря денег — обрыв ПОСЛЕ загрузки, во время
      решения** (не тот класс, что onAborted, и в логе НЕ виден). Дорогие
      вызовы vision/solver НЕ отменяются при обрыве клиента: `AbortController`/
      `signal` никуда не проброшен (проверено grep'ом vision.js/solver.js/
      anthropicClient.js), а флаг `closed` в стриме гасит только res.write в
      мёртвый сокет, не расход. Итог: если ученик закрыл webview за 60–150 с
      решения — мы платим за vision+solver. Смягчение: verified-решение всё
      равно кэшируется (setCached) → деньги дали переиспользуемый актив
      (тот же номер у одноклассника — бесплатно); ТЕРЯЕТСЯ полностью только
      неверифицируемое (гуманитарные). Частота не измерена (события нет ни в
      логе, ни в verify_events). Починка, если частота окажется значимой:
      проброс signal от res.on('close') в AI-вызовы (отмена на MAX-стороне
      тоже прекратит биллинг). Решение о починке — за Ильёй по данным.

## Чертежи геометрии (№12, заход 3; часть 1 сделана 31.08.2026)
Устройство принято Ильёй: детерминированные шаблоны по принципу группы Б
(модель называет параметры, чертит система; противоречие = отказ от чертежа),
представительные формы для задач «в общем виде» — заведомо НЕспециальные
(параллелограмм не близок к ромбу/прямоугольнику, треугольник разносторонний
и не близок к прямоугольному), проверка глазами на SVG обязательна.
- [x] Часть 1: прямоугольник/квадрат (поле drawing, блок «чертёж» у математики
      и геометрии, валидатор services/drawing.js, drawingSvg на фронте).
- [ ] **Грамматический бюджет structured output — учитывать ДО проектирования
      частей 2–4**: схема решения у предела грамматики structured output
      (8-полевой чертёж переполнил её: «compiled grammar is too large», 400 от
      API; влезло после сжатия до 6 полей). Поля для частей 2–4 проектировать
      в общем компактном виде — ОДНО поле на все фигуры, а не по полю на тип.
      **Замер бюджета на живом API (31.08.2026, ~$2, пробы с max_tokens=16;
      переполнение = 400 до генерации):** запас текущей схемы — меньше 5
      плоских строковых полей; каждый nullable-объект (drawing, visual, graph,
      answerValues) стоит ≥10 строковых полей; дешёвый schemaId (enum из 4)
      почти ничего не освобождает. Части 2–4 в текущую схему не влезают ни
      в каком виде. Проверенный путь: замена drawing единым полем фигур
      (kind-enum 7 видов + массивы values/labels/marks + comment) — влезает;
      слияние drawing+visual в это поле влезает с запасом ещё ≥5 строк.
      Решение Ильи (31.08): рефакторинг одним заходом — единое поле, куда
      сразу вливаются и visual, и drawing; обратная совместимость со старым
      кэшем обязательна (рендерить, а не прятать карточку).
- [ ] Часть 2: углы на прямых (смежные, вертикальные, параллельные + секущая).
- [ ] Часть 3: треугольник (по углам/сторонам, элементы, отметки равенства).
- [ ] Часть 4: четырёхугольники (параллелограмм ± диагонали, ромб, трапеция).
- Наблюдение (31.08, часть 3): критерий чертежа срабатывает стохастически —
  на «равнобедренный, угол при вершине 40°» модель 1 раз из 3 не дала figure.
  Осознанно НЕ ужесточаем (перерегулирование опаснее пропуска).
  **Триггер возврата к формулировке критерия: замер с судьёй показывает
  пропуск чертежа чаще чем в четверти геометрических задач.**
- Не в задаче: штриховка области под графиком (расширение механизма графиков),
  окружность (ждёт живого спроса — замера геометрии 9 класса).

## Многозадачность в check-пути (заглушка с 01.09.2026)
- [x] Временный регэксп-детектор помеченных списков («а)», «№8:») в
      checkHomework → честный unsupported «на листе несколько задач»;
      закрыл живой ложный FALSE (ma-03). Непомеченные списки (ma-09)
      остаются риском ложного FALSE — детектору не видны.
- [ ] Правильный слой — vision: расширить studentWork-схему полем tasks[]
      (как в task-режиме), промпт vision для разметки заданий в тетради,
      канарейки на реальных фото многозадачных работ (ma-01..05 подходят).
- [ ] Продуктовый вопрос: «какую задачу листа проверяем» — аналог экрана
      выбора задачи из solve-пути (renderTaskList), но для проверки; без
      него vision-разметка не дойдёт до пользы.

## Шаг 3 (парсер ученических ответов) закрыт — 01.09.2026
Итог: ответ ученика — число, а не выражение (строгая грамматика литерала);
процентные формы → /100 до вырезания кириллицы; «Ответ:»-префикс, хвостовые
точка/«руб.»/°/²; смешанные дроби в обеих формах vision («8 1/6» и
«8\frac{17}{51}»); заглушка многозадачности двумя правилами (метки пунктов
+ столбики примеров) с честной причиной для ученика.

- **Выборка: 10 живых листов — мала.** Пересмотр шага 3 при накоплении
  100 живых check-homework. Ответственный — Илья.
- **ГЛАВНАЯ НАХОДКА: 6 из 10 листов многозадачные, 8 из 10 упираются в
  незнание структуры листа.** Это не краевой случай, а ОСНОВНОЙ сценарий,
  под который check-путь не спроектирован: vision не размечает задания
  тетради, выбора «какую задачу проверяем» нет.
- Остаточные риски (задокументированы в коде): ma-10-класс — одиночный
  ответ без меток от другой задачи листа (лечится только vision-слоем);
  непомеченные списки ответов без «=» — неотличимы от множества корней.
- Метрики линии verify целиком (шаги 2–3): verified на 65 точных задачах
  22 → 42; ложных FALSE — 0; плавающих — 0–1; пять классов сбоев закрыты
  правилами, а не случаями (float-класс, коллизии имён, дублирование
  фильтров, тихие ложные verified, «строка ученика как выражение»).

## Телеметрия вердиктов (сделано 01.09.2026)
- [x] Таблица verify_events: route/source/вердикт/answer_kind/multi_task/
      parse_failure_kind/invariant_violation/duration_ms/error_kind.
      НЕ хранится: фото, тексты работ, ответы ребёнка, max_user_id —
      намеренно (см. комментарий в schema.sql).
- [x] Fire-and-forget из обоих роутов; при оборванной БД вызов возвращается
      за ~6 мс, процесс жив (проверено прогоном на мёртвый порт).
- [x] Счётчик триггера пересмотра парсера:
      SELECT count(*) FROM verify_events WHERE route='check' AND source='remote'
- Уточнение старого пункта «migrate на проде»: отдельного прод-сервера нет,
  прод-БД = локальная gdz_max за туннелем — миграции выполняются здесь же;
  пункт станет актуален только при переезде на постоянный хостинг.

## Факты о наборе и work-дублировании (01.09.2026, после vision-разметки тетрадей)
- В eval-наборе из 10 листов ОДНОЗАДАЧНЫХ НЕТ ВОВСЕ: vision-разметка даёт
  10/10 многозадачных (от 2 до 7 задач на лист; прежняя оценка «6 из 10
  по меткам» занижена, классификация ma-07 как одиночного была неверна).
  Следствие для UX проверки: выбор задачи — основной путь, не редкий.
- Вопрос «отдавать ли work при единственной задаче» решается ТОЛЬКО
  телеметрией на живых фото (в наборе не на чем) — не раньше.

## П.5 (сверка expression-ответов) — отложен до живых данных (31.08.2026)
В накопленных 543 решениях: expression на вычислимых предметах — 11, все
химия (уравнения реакций — SymPy не возьмёт, а отдельный движок баланса
11 случаями не окупается); серий корней и интервалов — ноль.
**Триггер возврата (проверять при пересмотре шага 3, на сотне живых
check-проверок):** в verify_events по вычислимым предметам набралось
30+ unsupported с kind=expression, ИЛИ среди них появились серии корней
и интервалы (алгебра 10–11). Ответственный — Илья.

## Очередь после стриминг-саги (31.08.2026, порядок Ильи; НЕ начато)

1. **Уведомления об ошибках** — бот пишет в личку в MAX при сбоях.
   Классы: падение бэкенда/ngrok; ошибки API Anthropic (кредиты, лимиты,
   5xx); ошибки vision/solver/compare; таймауты с недоехавшим решением;
   падение записи в БД. Требования: агрегация против спама, дедупликация
   повторов, отдельный сторож health (уведомление должно доходить, когда
   лежит то, о чём оно уведомляет), ошибки живых пользователей — отдельно
   от фоновых.
2. **Автоподъём** — node и ngrok на автозапуск с перезапуском при падении;
   сюда же: СТОРОЖ РАБОТОСПОСОБНОСТИ, НЕ НАЛИЧИЯ ПРОЦЕССА — урок 01.09:
   поллер бота умер (залипший сетевой пул после сетевого моргания), при этом
   процесс был жив и health отвечал бы ok — сторож «жив ли процесс» ничего
   бы не заметил. Проверять надо функции: успешный цикл поллера за последние
   N минут, успешный пробный solve, доехавший часовой отчёт. Канал
   уведомления — внешний (сам процесс о своей смерти не сообщит);
   фиксированный домен ngrok (смена адреса теперь стоит полного цикла
   деплоя + до 10 минут CDN-эджа — см. кэш-бастинг).
   Вес пункта вырос 31.08: фоновый бэкенд, запущенный из сессии Claude Code,
   был убит обвязкой фоновых задач сессии — прод за ngrok лежал до ручного
   перезапуска (теперь nohup+disown, но это держится на памяти, не на системе).
   Факты разведки launchd (31.08, сам launchd не делался):
   - **Автоподъём ngrok без фиксированного домена ОПАСЕН**: тихий рестарт
     молча сменит адрес и убьёт фронт до полного цикла деплоя (+CDN).
     Автоподъём и фикс-домен — только одним заходом, разделять нельзя.
   - **Закрытая крышка = лежащий прод при любом автоподъёме**: launchd не
     решает проблему сна машины. Следствие: автоподъём не заменяет хостинг,
     а только чинит падения процессов.
3. **Аудит сквозных путей** — все пользовательские сценарии: идут ли их
   канарейки тем же транспортом и с теми же заголовками, что webview,
   или в обход. Повод: два дефекта подряд прятались за curl (?cb=
   маскировал кэш, отсутствие preflight маскировало CORS-дефект).
4. **Разбор телеметрии (= «следующая задача» из session-state, единый список
   здесь)** — числа 10.09.2026, после недели живого трафика: реальные ошибки,
   длительности, unsupported, доля verified и кэш-хитов на живом, приходы по
   метке поста (post_20260831), решение по кэшу непроверенных решений и по
   гейтингу (появился ли живой not_subscribed). Оптимизации — только по этим
   данным, не раньше.

Ждём: прогон Ильи с телефона на фронте 2e51416 → app_version и transport
ответят, работает ли стриминг в webview MAX.

## Итог стриминг-саги (31.08.2026) — закрыто за последние заходы

Продукт: стриминг шагов решения (NDJSON, общее ядро с /api/solve,
проверенный фолбэк на POST) + ранний экран подтверждения сразу после
vision + пословная печать шагов (атомарные формулы/таблицы/блоки, очередь
с ускорением) — подтверждено Ильёй с телефона. Таймаут solve 60→150 с
(двое живых из семи умирали при готовом решении). №12 закрыт на две трети:
таблицы, текстовые формы (группа А), счётные рисунки (группа Б), пилот
готовых схем (В, 4 темы — ждёт глаз Ильи и первого замера с судьёй).

Инфраструктура доверия: телеметрия transport (stream/post/fallback),
app_version (X-App-Version с каждым запросом), text_edited, канареечный
source (X-Canary); кэш-бастинг с авто-версией (scripts/deploy-pages.sh —
единственный способ деплоя, отказывается от грязного webapp); версия фронта
видна на экране и в консоли. Починены два своих же дефекта доставки:
частичный деплой (новый HTML со старым app.js) и CORS-preflight
(X-App-Version не был в allowedHeaders).

## Отложенное — счётчики триггеров (31.08.2026)
- **П.5 (expression-сверка): ТРИГГЕР СРАБОТАЛ** — вторая ветка («появились
  серии/интервалы на алгебре 10–11») подтверждена шестью живыми запросами
  Ильи (kind=expression, «серия, интервал, именованные части»); первая
  ветка 15/30. Решение о возврате — за Ильёй.
- Пересмотр шага 3: 0/100 живых check (check remote ещё не начался).
- «Неверная побочная ремарка»: 2/3 — при третьем чинить.
- №13 (брак генерации, смешение алфавитов): 3 случая за замеры.
- Каталог схем (В): по результатам пилота на живых замерах с судьёй.
- Очередь работ: уведомления о сбоях → автоподъём+фикс-домен → аудит
  сквозных путей → разбор телеметрии 10.09 (см. выше, не начато).
