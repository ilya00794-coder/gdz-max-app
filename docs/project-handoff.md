# Хэндофф: интеграция с MAX (Bot API, gating)

Файл создан 29.08.2026 при реализации gating — раньше не существовал.
Здесь факты, добытые диагностикой, чтобы не переоткрывать их заново.

## Bot API: рабочая конфигурация
- **Хост: `platform-api.max.ru`** (переопределяется `MAX_API_HOST`). Документация
  рекомендует `platform-api2.max.ru`, но его сертификат выпущен «Russian Trusted
  Sub CA» (Минцифры) и не проходит проверку с dev-машины. На проде с установленным
  корнем Минцифры можно переключиться на api2.
- Токен — **заголовком `Authorization`**; параметр URL `access_token` отвергается
  с `verify.token`.
- Лимит: 30 rps на всё API бота; при 429 читать `Retry-After`.

## Канал проекта
- «Домашка в МАХ», max.ru/id772408566819_biz, бот — администратор.
- **`MAX_CHANNEL_CHAT_ID=-74564545637740`** (в `.env`; держать СТРОКОЙ — число у
  предела точности Number). Добыт из события `bot_added` через long polling:
  по ссылке chatId получить нельзя, `GET /chats` устарел с июня 2026.
  Инструмент: `npm run catch-chat-id` (backend/src/tools/catch-chat-id.js).

## Проверка подписки (проверено на живом API 28–29.08.2026)
- `GET /chats/{chatId}/members?user_ids=<id>`:
  - подписан → **200 + запись в members**;
  - не найден → **200 + пустой members** (не 404!);
  - 404 остаётся признаком неверного chatId, т.е. ошибкой конфигурации.
- Метод возвращает ОБЫЧНЫХ подписчиков канала (48 из 50 без флагов), не только
  админов. В записи ChatMember НЕТ поля статуса (member/left) — только
  is_owner/is_admin/is_bot; участие = присутствие в ответе.
- **НЕ проверено: существующий аккаунт, не подписанный на канал** (тест был на
  несуществующем id). Закрывается shadow-режимом на реальном трафике.
- Пагинация marker не проверена (канал < 100 подписчиков, одна страница).

## Gating (backend/src/subscription.js)
- `SUBSCRIPTION_GATING`: `off` (по умолчанию) / `shadow` (решение логируется,
  никто не блокируется) / `on` (not_subscribed → 403, ошибка → fail-open).
- Кэш: subscribed 15 мин, not_subscribed 60 сек, error не кэшируется; после 429 —
  backoff по Retry-After.
- Интерлок: `on` требует `STRICT_INIT_DATA=on`, иначе сервер не стартует —
  без подписи initData userId подделывается заголовком.
- Старт-проверка `GET /chats/{id}`: не 200 или type≠channel → громкая ошибка в лог
  (неверный chatId при fail-open молча открыл бы доступ всем).
- **Текущее состояние (29.08.2026): shadow включён через backend/.env** и переживает
  перезапуски. Ждём в логах строку `decision: not_subscribed` от РЕАЛЬНОГО
  пользователя — это последний непроверенный факт перед включением on
  (тесты гонялись на несуществующем id, живой неподписанный аккаунт не попадался).

## MAX Bridge / initData (webapp + middleware, проверено ранее)
- Скрипт st.max.ru/js/max-web-app.js создаёт window.WebApp ДАЖЕ вне MAX —
  признак настоящего клиента только непустой initData (фронт так и определяет режим).
- Подпись initData: secret = HMAC-SHA256("WebAppData", MAX_BOT_TOKEN), затем подпись
  отсортированных a→z параметров, склеенных \n. Сверено с реальной строкой с телефона.
- STRICT_INIT_DATA (off по умолчанию): on → 401 без валидной/свежей подписи.
  Локальные запросы отличаются НЕ по адресу (ngrok проксирует на 127.0.0.1),
  а по отсутствию X-Forwarded-For.

## Напоминания для прода
- npm run migrate на проде НЕ выполнялся: таблицы feedback там нет,
  POST /api/feedback будет падать 500, пока не прогнать миграцию.
- Адрес ngrok одноразовый: после перезапуска туннеля обновить webapp/config.js
  и передеплоить Pages. Токен ngrok, засветившийся в переписке, отозвать.
- **Экран настройки теперь ЗАВИСИТ от доступности бэкенда** (с 30.08.2026):
  список предметов приходит с GET /api/subjects, и при лежащем туннеле
  пользователь не увидит предметов вообще — приложение мертво с первого
  экрана, а не только на решении. Ещё один аргумент за постоянный сервер
  вместо ngrok (см. «Хостинг» в backlog).
- Включение STRICT_INIT_DATA=on и SUBSCRIPTION_GATING=on — осознанный шаг
  (см. интерлок), сначала прогон shadow на реальном трафике.

## Замер качества OCR (29.08.2026, инструмент ocr-eval)
- 9 фото русской рукописи + 10 математических (1-11 класс), эталоны черновые
  (писались той же моделью — коррелирующие ошибки невидимы, цифры = нижняя оценка).
- Чистый почерк читается почти без ошибок в числах; тёмные/небрежные фото сыплются.
- **ГЛАВНЫЙ ВЫВОД: confidence как сигнал ненадёжности цифр НЕ работает** —
  ma-02 имел confidence 0.78 при восьми цифровых ошибках (1,38 пять раз прочитано
  как 1,58). Корреляция confidence/ошибки на выборке: Пирсон −0.36. Гейтов на
  confidence тоньше существующего порога 0.4 не строить.

## Misread-детектор (backend/src/services/misread.js)
- Флаг MISREAD_DETECTION (сейчас on в .env, лог-only): после vision, параллельно
  solveTask, fire-and-forget — пользователь не ждёт. Числовые цепочки равенств
  из работы ученика сверяются через verify_sympy (числа в Rational — иначе
  python-float даёт ложные 10⁻¹³-расхождения).
- ДВА ограничения по построению: (1) видит только числовые равенства — переменные,
  неравенства, столбики-аннотации мимо; (2) слеп к систематической подмене:
  ловит misread, только когда тот ЛОМАЕТ соотношение. «1,38→1,58 везде + итог 158»
  не поймался бы; ma-02 пойман из-за ВТОРОЙ ошибки (37,5→57,5 сломало сумму =100).
- Флаг НЕ различает misread и настоящую ошибку ученика — показывать пользователю
  как «ошибка распознавания» нельзя. Числа никогда не исправляются.
- Стоимость: по одному SymPy-процессу на пару, последовательно, в фоне
  (13 пар ma-07 = 13 процессов по ~0.25 с; пользователь не ждёт).

## Замер Opus vs Haiku на solver (30.08.2026, инструмент model-compare)
- 110 собственных типовых задач, 1–11 класс, промпт/curriculum одинаковые,
  менялась только модель. Итог после ручного разбора расхождений:
  **обе модели не сделали ни одной математической ошибки**. Opus 110/110;
  Haiku 109/110 + один отказ (производная в 10 классе: её нет в белом списке
  методов — Haiku честно отказалась, Opus решил обходом через приращения).
- **Haiku втрое дешевле**: $0.0087 против $0.0252 за задачу (с поправкой на
  прогрев кэша, проверено по usage-токенам). Скорость сравнивать нечестно:
  Haiku шла с фиксированным бюджетом мышления 8000 вместо adaptive
  (модель до 4.6 не умеет adaptive/effort) — это шим инструмента.
- Вся разница — дисциплина формата finalAnswer (Haiku пишет «1/2 (или 0,5)»,
  LaTeX) и трактовка границ программы, а не правильность.
- **НЕ проверено**: многошаговые задачи — вторая часть ОГЭ/ЕГЭ, геометрия
  с построением, доказательства. Вывод туда не переносится.
- Модель в проде НЕ менялась.
- Побочный улов замера — три живых бага продукта, все починены с канарейками
  до/после: parseCandidateAnswer не расщеплял по «или»/«и» (JS-\b не работает
  с кириллицей → «x = 2 или x = 3» проваливал верификацию и кэш), NO_ROOTS
  не знал «не имеет действительных корней», numbersToRationals давал
  SyntaxError на «0.3» (Rational(03,10), ведущий ноль).

## Предметы по классам (30.08.2026, проверено на телефоне)
- Список предметов на экране настройки зависит от класса и приходит с бэкенда:
  GET /api/subjects?grade=N (routes/subjects.js). Жёсткого списка на фронте
  больше НЕТ (и fallback'а на него тоже — намеренно): блок скрыт до выбора
  класса, на время запроса — чипы-скелеты, при смене класса невалидный предмет
  сбрасывается, при обрыве сети — сообщение и «Повторить». Гонка быстрых
  кликов по классам гасится счётчиком запросов.
- Пара (класс, предмет) валидируется на бэкенде в solve.js И checkHomework.js
  по данным учебного плана (services/subjects.js): невозможная пара
  («3 класс + физика») → 400 с перечнем доступных предметов. Синонимы старого
  фронта («Английский», «Математика» в 7–11 как агрегат) принимаются.
- Данные: src/data/curriculum/subjects.json — привязаны к учебному году
  2026/27 (обществознание уже только в 9 классе) и требуют пересмотра при
  изменении ФУП (приказы об изменении ФОП на edsoo.ru). Источники и принятые
  решения — в src/data/curriculum/SOURCES.md.

## Графики функций (31.08.2026, проверено на телефоне)
- Разделение ролей: solver только НАЗЫВАЕТ функцию (поле graph.expressions,
  1–2 штуки для системы, плюс xRange и comment) — координат модель не
  касается; verify_sympy mode=plot считает 201 точку, нули, экстремумы,
  асимптоты (.subs(), не lambdify — см. комментарий в файле про модель
  угроз); фронт рисует своим SVG без библиотек (graphSvg в webapp/app.js).
- Критерий «когда график нужен» живёт в промпте solver (исследование
  функции, парабола/вершина, система как пересечение прямых, метод
  интервалов, область определения — и явные НЕ-случаи). Проверен
  канарейкой 8/8 и на телефоне: появляется где нужен, молчит где нет.
- Graceful-отказ: любой сбой расчёта (небезопасное выражение, синтаксис,
  таймаут 5 с, ok:false) = решение приходит БЕЗ графика, одна строка
  [graph] в логе. График — усиление, не условие.
- Точки кэшируются вместе с решением (+2–3 КБ на запись jsonb),
  кэш-хит отдаёт график без пересчёта. Расчёт идёт параллельно
  SymPy-верификации ответа — латентности не добавляет.
- Разрывы: y:null рвёт полилинию (гипербола — две ветви); окно по y —
  срединные 88% значений + ось x/экстремумы/асимптоты, границы
  округляются до шагов 1/2/5/10·10^k.

## Фикс верификации арифметики (31.08.2026)
- «128 + 236» вычислялось питоном в int, to_solution_list его не понимал —
  ВСЯ арифметика началки шла с method:unsupported, без бейджа «проверено
  вычислением» и мимо кэша. Теперь int/float → sympy.S → verified true.
