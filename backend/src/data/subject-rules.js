// Предметные блоки системного промпта solver.
//
// Зачем не в SYSTEM_BASE: общий промпт уже ~2000 токенов с тремя предметными
// ветвлениями, и мы на опыте видели, что мягкие инструкции в нём теряются
// (возрастная оговорка сработала только после явного запрета). Предметное
// правило должен видеть только тот предмет, которому оно адресовано.
//
// Подмешивается в solveTask ТРЕТЬИМ system-блоком между SYSTEM_BASE и
// программой класса — до cache_control, поэтому префикс по-прежнему
// кэшируется по тройке (класс, предмет, четверть), новых ключей кэша нет.
//
// BLOCKS — тексты правил; SUBJECT_TO_BLOCKS — какие блоки видит предмет
// (предмет может видеть несколько: литература = свой + общегуманитарный).
// Предмет без записи не получает ничего — общей базы ему достаточно.

const BLOCKS = {
  // Заполняются по одному с канарейкой на каждый (см. prompt-gaps-report):
  // осталось "литература", "гуманитарные".

  "английский": `АНГЛИЙСКИЙ ЯЗЫК — произношение и чтение (обязательные):
- К каждому английскому слову, которое появляется в решении впервые, давай
  произношение русскими буквами: went — «уэнт», dogs — «догз». МФА-транскрипцию
  одну не оставляй: младший школьник её не читает.
- Звуки, которых нет в русском, передавай приближённо и ЧЕСТНО помечай это:
  th (three, think) — межзубный звук, у него НЕТ точного русского аналога,
  ближе всего «с» с языком между зубами — НЕ пиши просто «фри» или «сри»;
  w (water, went) — губы трубочкой, звук между «у» и «в»;
  краткие гласные (bit/beat) — оговаривай долготу.
- С 5 класса рядом с русской подсказкой добавляй транскрипцию в квадратных
  скобках: three [θriː] «сри-и (межзубное с)» — русская подсказка остаётся.
- Если работает правило чтения — назови его: открытый/закрытый слог
  (name — «нейм»: открытый слог, a читается «эй»), буквосочетания
  (ee — долгий «и», sh — «ш», ck — «к»).`,

  "геометрия": `ГЕОМЕТРИЯ — правила изложения (обязательные; ученик НЕ видит чертежа):
- ДО решения введи обозначения: назови вершины, точки и углы буквами
  («треугольник ABC», «точка D на стороне AC», «обозначим углы при
  пересечении ∠1, ∠2, ∠3, ∠4») — и опиши конфигурацию словами так, чтобы
  по одному тексту её можно было НАЧЕРТИТЬ: что где лежит, какие лучи и
  прямые, что чему параллельно или перпендикулярно, что между чем.
- Дальше ссылайся ТОЛЬКО на введённые обозначения. Запрещены отсылки
  «соседний с ним», «напротив данного», «этот угол» — вместо них ∠1,
  ∠ADB, сторона AB.
- В задачах на доказательство первый шаг — «Дано: … Доказать: …» в
  обозначениях; каждый переход обосновывай НАЗВАННОЙ теоремой, признаком
  или свойством («по третьему признаку равенства треугольников»).
- Ключевые взаимные расположения проговаривай явно ДО того, как ими
  пользоваться: если точка D лежит на отрезке AC между A и C — сначала
  скажи это, и только потом выводи, что лучи DA и DC дополнительные,
  а углы ∠ADB и ∠CDB смежные.`,

  "физика": `ФИЗИКА — правила оформления вычислений (обязательные):
- Единицы измерения пиши В КАЖДОЙ подстановке чисел, а не только в «Дано»
  и ответе: «v = 36 км / 2 ч = 18 км/ч» — НЕ «v = 36/2 = 18».
- Перевод единиц показывай С ВЫВОДОМ, а не готовым множителем:
  «1 см² = 0,01 м · 0,01 м = 0,0001 м², значит 3000 см² = 0,3 м²» —
  НЕ «умножаем на 10⁻⁴».
- Перед ответом отдельно проверь размерность: распиши, как единицы
  сокращаются до единицы ответа, например «кг/м³ · Н/кг · м³ = Н».
- В 7–8 классах g — это коэффициент 10 Н/кг, как в учебнике; термин
  «ускорение свободного падения» используй только с 9 класса.`,
};

const SUBJECT_TO_BLOCKS = {
  "физика": ["физика"],
  "геометрия": ["геометрия"],
  "английский язык": ["английский"],
  "английский": ["английский"],
  "литература": ["литература", "гуманитарные"],
  "литературное чтение": ["литература", "гуманитарные"],
  "история": ["гуманитарные"],
  "обществознание": ["гуманитарные"],
  "география": ["гуманитарные"],
  "биология": ["гуманитарные"],
};

/** Та же нормализация, что в curriculum.js/subjects.js. */
function normalize(subject) {
  return String(subject || "").trim().toLowerCase().replace(/ё/g, "е");
}

/**
 * Текст предметных правил для system-блока. Пустая строка — блока нет,
 * solveTask в этом случае третий блок не добавляет вовсе.
 */
export function subjectRules(subject) {
  const keys = SUBJECT_TO_BLOCKS[normalize(subject)] ?? [];
  return keys.map((k) => BLOCKS[k]).filter(Boolean).join("\n\n");
}

// Только для канареек: даёт тестам подложить временный блок, не трогая прод.
export const _testOnly = { BLOCKS };
